services:
  - type: web
    name: hwyw-tetris
    runtime: node
    plan: free
    buildCommand: "echo skip"
    startCommand: "node server.js"
    healthCheckPath: /api/health
    envVars:
      - key: PORT
        value: "3000"
